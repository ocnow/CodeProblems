fn main() {
   let bul : bool = contains_nearby_duplicate(vec![10,12], 2);
    println!("{bul}");
}

pub fn contains_nearby_duplicate(nums: Vec<i32>, k: i32) -> bool {
    let indexAr :[i32;10000];
    let mut index = indexAr.len();
    while index != 0 {
        let temp = nums[index];
        println!("{temp}");
        index-=1;
    }
    return true;
}
